'use strict';

function youtube_parser() {
    let url = $('#show-link').val();
    var regExp = /^.*((youtu.be\/)|(v\/)|(\/u\/\w\/)|(embed\/)|(watch\?))\??v?=?([^#\&\?]*).*/;
    var match = url.match(regExp);
    if ((match && match[7].length == 11)) $("#link").val(match[7]);
}